import { RepeatValidatorDirective } from './repeat-validator.directive';

describe('RepeatValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new RepeatValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
